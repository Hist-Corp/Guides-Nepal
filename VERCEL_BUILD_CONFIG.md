# VERCEL_BUILD_CONFIG.md (archived)

Consolidated into **[DEPLOYMENT.md](./DEPLOYMENT.md)**. For both `frontend` and `dashboard` Vercel projects: Install 
pm install`, Build 
pm run build`, Output `dist`, Root Directory set to the respective sub-folder.
